import 'package:fhds/fhds.dart';
import 'package:flutter/material.dart';

class DesignSystemCardsView extends StatelessWidget {
  const DesignSystemCardsView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cards'),
      ),
    );
  }
}
